about:
  gist:
    id: String
    file: String

commits:
  polling:
    enabled: Bool
    interval: Duration
    limit: Int

availability:
  gcal:
    calendarIDs:
      - String # Calendar ID

music:
  polling:
    enabled: Bool
    interval: Duration

location:
  polling:
    enabled: Bool
    interval: Duration

  region:
    # One of:
    #  - Country
    #  - State
    #  - County
    #  - City
    #  - District
    #  - Postcode
    geocodeLevel: GeocodeLevel

  airtable:
    baseID: String
    table: String
    view: String

  here:
    appID: String

shutdownTimeout: Duration
