about:
  gist:
    id: String
    file: String

commits:
  limit: Int
  pollInterval: Duration

availability:
  gcal:
    calendarIDs:
      - String # Calendar ID

music:
  pollInterval: Duration

location:
  airtable:
    baseID: String
    table: String
    view: String
  here:
    appID: String
  historian:
    timezone: String
  pollInterval: Duration

shutdownTimeout: Duration
