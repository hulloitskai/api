about:
  gist:
    id: String
    file: String

commits:
  limit: Int
  pollInterval: Duration

availability:
  gcal:
    calendarIDs:
      - String # Calendar ID

nowPlaying:
  pollInterval: Duration

shutdownTimeout: Duration
