about:
  gist:
    id: String
    file: String

commits:
  polling:
    enabled: Bool
    interval: Duration
    limit: Int

availability:
  gcal:
    calendarIDs:
      - String # Calendar ID

music:
  polling:
    interval: Duration

location:
  polling:
    enabled: Bool
    interval: Duration

  airtable:
    baseID: String
    table: String
    view: String
  here:
    appID: String
  historian:
    timezone: String

shutdownTimeout: Duration
