package internal

import (
	"go.stevenxie.me/api/internal"
)

const (
	// Name is the command name.
	Name = "server"

	// Namespace is the command namespace.
	Namespace = internal.Namespace + "." + Name
)
